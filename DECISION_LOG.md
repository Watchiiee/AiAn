---

## J. LangGraph 심화 (Adaptive RAG) 및 후속 개선

### J-1. 3분기 라우터 + Document/Hallucination/Answer Grader + 재시도 루프
**결정**: 기본 LangGraph 파이프라인(classify→rule→retrieve→generate)을 확장해
urgent/chitchat/rag 3분기, 검색 근거 채점(doc_grader) + 재검색 루프(최대 2회),
환각 검증(hallucination_grader) + 적합성 검증(answer_grader) + 재생성 루프
(최대 2회, 두 grader 공유)를 추가.
**근거**: "이 프로젝트는 담당자 검토 후 발송하므로 응답속도보다 정확도 우선"
이라는 전제(기존 결정) 위에서, LLM 호출 증가(문의당 최대 9회 이상)를 감수하고
검증을 다단계로 강화. 각 grader는 독립된 짧은 프롬프트로 분리해 역할 혼동을
방지 (분류 프롬프트에 여러 필드를 한 번에 넣었을 때 필드가 혼동됐던 전례 반영).
**안전장치**: 재시도가 소진돼도 무한루프 대신 `finalize` 노드에서 확신도를
자동으로 하향(sufficient→partial)해 검증 실패를 숨기지 않고 담당자에게 신호를 준다.

### J-2. 겪은 버그 — CLOVA의 마크다운 코드블록 래핑
**문제**: 프롬프트에 "코드블록 금지"를 명시했음에도 CLOVA가 종종
` ```json ... ``` ` 으로 감싸서 응답. `json.loads()`가 파싱 실패 → 예외 →
조용히 키워드 폴백으로 전환 → 폴백은 안전을 위해 `is_relevant=True`로
고정해두어, 실제로는 chitchat(잡담)이었던 문의가 rag 경로로 흘러 들어가
`no_evidence`로 끝나는 현상 발생(겉보기엔 비슷한 결과라 원인 파악에 시간이 걸림).
**해결**: 프롬프트 지시만 믿지 않고, JSON 파싱 직전에 코드블록을 벗겨내는
방어 함수(`_strip_code_fence`)를 classifier·grading·generator 세 곳 모두에 추가.

### J-3. 겪은 버그 — chitchat인데 "부서 재배정 필요" 신호가 잘못 붙음
**문제**: 잡담(is_relevant=False)도 category=OTHER로 분류되어, rule_engine의
기본 로직상 `department_certain=False`(재배정 필요) 플래그가 붙음. 그런데
잡담은 원래 배정할 "맞는 부서"가 없는 경우라 "재배정하라"는 신호 자체가 안 맞음.
**해결**: `chitchat_decline` 노드에서 RuleResult를 `department_certain=True`
(이 판정 자체는 확실함) + 구분되는 안내문구로 덮어써, "애매한 업무 문의(재배정 필요)"와
"업무 무관 잡담(재배정 불필요)"을 명확히 구분.

### J-4. 겪은 버그 — department_note가 DB에 저장되지 않아 API 응답 단계에서 사라짐
**문제**: 파이프라인이 만든 실제 안내문구(`department_note`)를 저장하는 DB
컬럼이 없어서, `/pending` 응답 생성 코드가 `department_certain` 불(bool) 값만
보고 문구를 그때그때 재조합(단순 규칙: certain=true면 무조건 null)하고 있었음.
그 결과 J-3에서 공들여 만든 문구가 조회 시점에 사라지고 null로 대체됨.
**해결**: `department_note` 컬럼 추가, 저장 시 실제 값 보존, API 응답도 저장된
값을 그대로 반환(재조합 금지).
**교훈**: API 레이어에서 상태값(bool 등)만 보고 표시용 텍스트를 즉석 재생성하면,
그 값을 만든 하위 로직의 의도가 조용히 사라진다. 텍스트 자체를 저장하고
그대로 내려주는 것이 안전하다.

### J-5. 우선순위(기급) 판정 방식 재검토 — 키워드 매칭 폐기
**초기 방식**: 본문에 "마감/당장/즉시/불만/항의/피해" 등 키워드가 있으면
무조건 우선순위를 "긴급"으로 승격, 그래프도 이 키워드로 urgent 경로 분기.
**문제 제기 및 재검토**: 키워드 매칭은 "긴급! 저랑 사귈래요?" 같은 문장에서
단어의 문자적 존재만으로 오판(거짓양성)할 위험이 있음. 반대로 "정전으로
병원 수술실이 멈췄다" 같은 진짜 긴급 상황은 키워드가 없으면 못 잡음(거짓음성).
**결정**: 키워드 매칭을 완전히 제거. 대신 독립된 LLM 호출(`check_urgency`
노드)이 문맥으로 긴급 여부를 판단하고, **구체적 근거(urgent_reason)가 있을
때만** 긴급으로 인정한다(`is_urgent=True`인데 근거가 빈 문자열이면 기각).
이 근거는 DB에 저장되어 담당자 화면(`/pending`)에 노출되므로, AI의 판단을
사람이 검증할 수 있다(블랙박스 판정 금지 원칙).
**의의**: "판단은 근거와 함께, 규칙은 판단이 아니라 기본값에만" 이라는
일관된 원칙을 시스템 전체(도메인 분류, 부서 매핑, 이제 긴급도까지)에 적용.

---

## K. 기술 지식베이스 대규모 확충 및 검색 품질 검증

### K-1. technical 도메인 대규모 확충 (18개 파일, 431청크)
**배경**: J절까지는 technical 도메인이 컨테이너 리셋 이전 상태 기준 거의 비어있었음.
멘토 자료(실제 KEEA 기술질의 회신 원문, 카테고리별로 LA/SA·MCCB/ELB·VCB/ACB·
계전기·발전기·변압기·전동기·접지·조명·케이블·콘덴서·퓨즈 등 총 700개 이상의
Q&A)를 순차적으로 markdown 지식베이스로 변환해 investing.
**변환 원칙**: ① Q당 하나의 `### Q.` 헤더(청크 단위와 일치), ② 각 청크 700자 이내로
유지(자동분할 시 표·문장 중간이 끊기는 것 방지), ③ 원본에 실제 답변이 없는
("첨부파일 참조"뿐인) 항목은 제외, ④ 여러 원본에 중복 등장하는 항목은 주제상
가장 맞는 파일 하나에만 배치(예: VCB/ACB 원본 배치에 있던 배선용차단기 용량산정
질문은 MCCB/ELB 파일로 이동).
**도메인 분리**: 일부 원본(전기설비기술기준, 전력량계)은 내용이 "안전관리자
법적 책임"(admin)과 "설치기준 수치"(technical)로 성격이 섞여 있어 각각 admin/
technical 파일로 분리해 넣음.

### K-2. 검색 품질 배치 테스트 도입
**결정**: 대량 콘텐츠 투입 후 수동으로 하나씩 확인하는 대신, 24개 질문(파일별
존재확인 19개 + 크로스체크 1개 + 도메인라우팅확인 2개 + 회귀확인 2개)을 한 번에
돌려 표로 결과를 뽑는 `scripts/test_retrieval_quality.py`를 작성.
**결과**: 19개 파일 존재확인 중 17개 정확 매칭(1개는 도메인분류 오류로 인한 실패,
1개는 검색 자체가 안 됨). 크로스체크(인버터 모터 접지선 질문이 전동기 파일이
아닌 접지 파일에서 검색되는지)는 통과 — 유사 주제가 여러 파일에 흩어져 있어도
정확한 파일이 우선 검색됨을 확인.

### K-3. 겪은 버그 — hallucination_grade가 "여러 문서 종합"을 지어낸 내용으로 오판
**문제**: 검색된 문서가 2개 이상이고 답변이 그 문서들을 종합한 경우,
`hallucination_grade`가 반복적으로(최대 재시도 3회 모두) false를 반환해 확신도가
sufficient→partial로 강등되는 사례 발견("전력량계가 어떻게 유효전력만
측정하나요?" 질문). Langfuse 트레이스로 실제 생성된 답변 3개를 근거 문서와
직접 대조한 결과, 답변에 지어낸 내용은 전혀 없었음 — grader가 "여러 문서를 합쳐
답한 것"을 "문서 하나에 없는 내용"으로 오인한 것으로 확인.
**해결**: `HALLUCINATION_GRADER_SYSTEM` 프롬프트의 판단 축을 "표현"에서 "사실"로
명확화. 여러 문서 종합·의역·요약은 문제 삼지 않되, 구체적 사실·수치·조건이
문서와 다르게 바뀐 경우만(예: "약 3%"→"약 5%", "가능한 경우가 많다"→"항상
가능하다") false로 판정하도록 기준을 재작성. **주의**: 이건 grader를 "관대하게"
만든 것이 아니라 "정확하게" 만든 것 — 지어낸 내용을 걸러내는 엄격함은 그대로
유지하면서, 표현 차이로 인한 오탐만 제거함. 수정 후 동일 질문 재실행 시 재시도
없이 1회에 hallucination_grade=true, answer_grade=true로 통과해 sufficient
확신도가 안정적으로 나오는 것을 확인.

### K-4. 발견된 이슈와 해결 — 프로즈 보강 및 도메인 분류 명확화
- **고조파 규정표 검색 미스**: `knowledge_harmonics.md`의 표 위주 청크(마크다운 pipe table)가
  임베딩 검색에서 순위가 밀리는 현상 확인. 표 앞에 같은 수치를 자연어 문장으로 먼저
  서술하는 "프로즈 보강"을 적용(표는 그대로 유지, 사람이 보기엔 표가 명확하고 임베딩은
  문장에서 의미를 더 잘 잡음). Hit@k로 5등 추정 → 2등으로 개선 확인.
- **도메인 분류 오류**: "산업용 계량기 폐지가 이득이 되나요?" 질문이 "계량기"라는 단어
  때문에 admin이 아닌 technical로 오분류됨. `CLASSIFIER_SYSTEM` 프롬프트에 "요금·신청
  맥락이면 계량기라는 단어가 있어도 admin" 기준과 대조 예시(요금 질문 vs 원리 질문)를
  추가해 해결.
- **영문 약어 검색 미스**: `knowledge_grounding.md`의 "TN-S/TT/IT 계통" 청크가 영문
  약어 위주라 임베딩에서 10등까지 밀림(한국어 일반 전기용어가 많이 겹치는 다른 파일이
  오히려 우선순위에 옴). 헤더와 본문에 "TN-S 계통", "TT 계통", "IT 계통"처럼 약어와
  한국어 설명을 더 가깝게 반복 서술하는 프로즈 보강 적용. Hit@k로 10등 → 1등 개선 확인.
- **공통 교훈**: 표·영문약어처럼 "구조화되거나 비한국어인 콘텐츠"는 문장임베딩
  모델(ko-sroberta)에서 의미 포착이 약해지는 경향이 있어, 원문 표기(표/약어)는 유지하되
  핵심 내용을 자연어 문장으로 한 번 더 반복 서술하는 것을 앞으로 지식베이스 작성 시
  기본 관행으로 삼기로 함.

### K-5. Hit@k 정량 평가 도구 도입 및 최종 검증
**결정**: "검색이 잘 되는지 느낌으로 판단" 하지 않고, `scripts/eval_hit_at_k.py`를 만들어
분류·생성·채점 등 LLM 단계 없이 `retrieve()`만 단독 호출해 정답 파일의 검색 순위를
숫자(Hit@1/3/5/10)로 측정하도록 함. classify() 오분류의 영향을 배제하기 위해 정답
도메인을 직접 주입해 순수 검색 품질만 측정.
**결과**: K-4의 수정 3건 적용 후 라벨링된 22개 질문 기준 **Hit@3 = 100%**(Hit@1 90.9%,
2건만 2등). 실제 서비스 설정(`top_k=3`)에서 라벨링된 질문 전부가 doc_grade의 후보군에
들어옴을 확인.
**판단**: 이 결과에 따라, 이전에 논의했던 "동적 top_k(점수격차 기반)"·"doc_grade를
선별형으로 업그레이드" 두 아이디어는 방향 자체는 맞지만 **지금 당장은 우선순위가
아니라고 판단**해 보류. 실사용 중 새로운 검색 미스 패턴이 관찰되면 그때 재검토.
MDEOF
echo done

---

## L. 검증 방법론 재검토 — 골든 데이터셋 우선 원칙으로 전환

### L-1. K절 검증 방식의 한계를 스스로 발견
K-5에서 만든 `scripts/test_retrieval_quality.py`(24개 질문), `scripts/eval_hit_at_k.py`
(22개 라벨링 질문)는 **직접 손으로 만든 질문 세트**였다는 근본적 한계가 있음이
드러남. 구체적으로:
- TN-S 청크를 "고쳐서 → 같은 테스트로 재확인 → 성공"하는 과정 자체가, 그 22개
  질문에만 맞춰 청크 문구를 튜닝하는 **오버피팅**이었음(사용자 지적으로 발견).
- `top_k`를 3→15로 늘리는 것을 "Hit@3=100%"라는 결과로 정당화하려 했으나,
  hit rate류 지표는 k가 커질수록 구조적으로 개선되므로 이 근거 자체가
  틀렸음(사용자 지적으로 발견, Claude가 인정).
- 이 두 스크립트와 그 결과 파일(`data/test_results/`)은 **깃허브에는 올리지 않고
  로컬에서 삭제**하기로 함(방법론적으로 불완전하다는 걸 알면서 커밋하지 않기 위함).
  다만 아래에 방법론과 핵심 발견은 보존한다.

### L-2. K절에서 실제로 확인된, 재사용 가능한 발견 (스크립트는 삭제하되 결론은 유지)
- **hallucination_grade 프롬프트 개선**은 유효함 — "여러 문서 종합/의역은 표현
  차이일 뿐 사실 왜곡이 아니면 true로 판단하라"는 기준 명확화는 실제 재시도
  횟수를 줄이고 정상 답변의 부당한 confidence 강등을 막음(이건 재현 가능한
  구조적 개선이라 유지).
- **표·영문약어 콘텐츠는 문장임베딩(ko-sroberta)에서 검색 순위가 밀리는 경향이
  있다**는 관찰 자체는 재검증 가치가 있음. 다만 "프로즈 보강으로 해결된다"는
  결론은 특정 22개 질문에서만 확인된 것이라 일반화 여부는 골든셋으로
  재검증 필요.
- **도메인 분류 프롬프트 개선**(계량기=요금이면 admin, 원리면 technical 구분
  예시 추가)도 특정 사례 하나에 대한 패치였음 — 골든셋으로 이런 유형의
  오분류가 얼마나 더 있는지 전반적으로 재점검 필요.

### L-3. 다음 단계 — 골든 데이터셋 우선, 그 다음 검색 구조 개선
**결정**: RAG 구조를 더 손대기(top_k 조정, doc_grade 개별선별화, 하이브리드
검색 등) 전에, **먼저 골든 데이터셋(질문-정답 문서/조문 쌍 80~100개)을 구축**하기로
함. 원본으로 확보한 700개 이상의 실제 KEEA Q&A에서 무작위 샘플링하는 방식을
우선 검토(손으로 새로 만드는 것보다 신뢰도가 높음).
**근거**: 골든셋 없이는 "top_k를 얼마로 할지", "엘보우(점수격차) 임계값을 얼마로
할지", "하이브리드 검색이 실제로 나은지" 전부를 매직넘버나 손튜닝으로 정하게 됨.
골든셋이 있으면 precision@k 등 k가 커져도 무조건 좋아지지 않는 지표로 실험할
수 있어 근거 있는 결정이 가능해짐.

**보류된 아이디어 (골든셋 확보 후 재검토)**:
- top_k를 고정값(3 또는 15)으로 결정하는 방식 — 근거 부족으로 보류.
- 엘보우(점수 격차 기반) 동적 k — 방향은 유효하나 임계값을 매직넘버로 정하지
  않고 골든셋 실험으로 산출하는 것을 전제로 재검토.
- doc_grade를 "전체 true/false" → "문서별 개별 선별(점수를 참고정보로 제공)"로
  업그레이드하는 설계는 방향 자체는 유지하되, 실제 구현은 골든셋으로 성능을
  측정할 수 있게 된 이후로 순서를 미룸.
- Elasticsearch 도입은 기각 유지(이전 결정과 동일 — 현재 규모(청크 수백~천개
  단위)에는 과도한 인프라). BM25 키워드 검색이 필요하다고 판단되면 별도 서버
  없이 경량 라이브러리(예: rank_bm25)로 구현하고, 벡터 검색 결과와는 RRF를
  애플리케이션 코드 레벨에서 적용하는 방향을 검토.
- 법령 원문(조문 전체 텍스트) 자체를 별도 컬렉션으로 추가하는 것 — 현재는
  Q&A 답변 문장 속에 조문 인용이 녹아있을 뿐, 조문 원문 자체는 지식베이스에
  없음. 필요성은 인지했으나 원문 확보 후 착수.

---

## M. 골든 데이터셋 Phase 1 — 구축 및 평가 결과

### M-1. Phase 1 골든셋 구축 (26문항)
`data/golden_dataset_phase1.csv`로 구축. 손으로 만든 22개 질문(K-5)의 오버피팅 문제를
넘어서기 위해, 이번엔 "위험도 기반 카테고리 선정"(harmonics·grounding — 검색미스
있었던 곳, mccb_elb — 대량 카테고리, motor — 대조군, admin 2개 파일 — 도메인 경계
이슈)에 목적별 유형(A 원문/B 의역/C 복합질문/E 약어형/F 경계형, D 법령전용은 원문
미확보로 보류)을 자연스럽게 배치. 컬럼: id/category/gold_domain/type/question/
source_file/gold_chunk_id/gold_answer/difficulty/notes. gold_chunk_id는 실제
파일의 `### Q.` 헤딩과 정확히 일치시켰고, C유형은 정답청크 2개를 ` + `로 결합해
recall 측정이 가능하게 함. (CSV는 UTF-8 BOM으로 저장 — 엑셀에서 한글이 깨지는
문제 해결.)

**부수적 발견**: 골든셋 라벨링 과정에서 `knowledge_safety_manager_liability.md`와
기존 `knowledge_electric_safety_manager.md`를 대조하다, 전자에 적어둔 "서류 보존
연한 3년"이 후자의 근거법령 명시된 "4년"과 불일치함을 발견해 즉시 수정(3년→4년,
근거: 전기안전관리법 제24조제3항 등). 두 파일이 함께 검색될 경우 LLM이 임의로
아무 숫자나 답할 위험을 사전에 제거함.

### M-2. `scripts/eval_golden_set.py` — 검색+분류 통합 평가
`retrieve()`만 단독 호출하는 검색 평가(정답 도메인 직접 주입, K-5와 동일 원칙)와,
`classify()`를 실제로 호출하는 도메인 분류 평가를 하나의 스크립트로 통합. 복합질문은
정답청크 2개 중 몇 개를 찾았는지 recall로, "전부 찾았을 때만" Hit@k로 집계.
(스크립트는 L-1 원칙에 따라 로컬 전용, 커밋하지 않음 — 결과와 방법론만 여기 기록.)

### M-3. 평가 결과
- **도메인 분류: 26/26 (100%), F유형(경계형) 3/3 (100%).** K-4에서 딱 1개 사례
  ("계량기 요금")로 고쳤던 `CLASSIFIER_SYSTEM` 프롬프트가, 전혀 새로운 F유형
  3개(계량기 CT비→admin, 계량기 CT배율 원리→technical, 안전관리자 사고책임→admin)
  에서도 전부 정확했음. K-5 당시 우려했던 "오버피팅"이 아니라 **일반화된 개선**임을
  확인.
- **검색: 평균 recall 90.4%, Hit@3 84.6%.** 다만 유형별로 뚜렷한 격차 발견 —
  단독질문(A/B/E, 21개)은 20/21(95.2%)로 거의 완벽했으나, **복합질문(C, 5개)은
  평균 recall 60%**로 눈에 띄게 약함(5개 중 2개는 recall 1.0, 2개는 부분실패,
  1개는 완전실패).

### M-4. 복합질문(C유형) 취약성 — 원인 조사 및 판단
원본 Q&A 424개 블록을 정규식으로 스캔(①②·1)2) 등 번호형 하위질문 vs 물음표 2개
이상)한 결과, **번호형(진짜 별개 주제 결합) 비율은 3.8%**로 드문 편(물음표
2개 이상은 18.9%이나 이는 같은 주제의 연속 질문도 포함해 과대추정 가능성 있음).
**판단**: query decomposition(복합질문을 하위질문으로 쪼개 각각 검색)은 방향은
유효하나, 발생 빈도가 낮아 최우선 과제로 격상하지 않고 백로그로 유지.

### M-5. G05(IEEE Std 142) 진단 — 반복되는 실패 패턴 재확인
"IEEE Std 142 기준으로 공통접지 접지저항은 몇 옴이어야 하나요?" 질문이 정답청크를
전혀 찾지 못함(단독질문 21개 중 유일한 완전실패). 진단 결과, 해당 청크의 헤딩
(제목)에는 "공통접지"만 있고 "IEEE Std 142"는 본문 중간에 다른 영문표준(ANSI/
NFPA)·숫자(Ω 여러 개)와 함께 묻혀 있음이 확인됨.
**의의**: 이는 K-4(고조파 표), K-5(TN-S 약어)에 이어 **세 번째로 확인된 동일
패턴**— "영문 약어·표준명이 헤딩에 노출되지 않고 숫자 밀집 본문에 묻히면 임베딩
검색에서 누락되기 쉽다"는 것이 일회성 우연이 아니라 재현되는 구조적 약점임을
뒷받침. **결정**: 개별 청크를 프로즈로 patch하는 임시방편 대신, L-3에서 보류했던
"BM25 키워드 검색 하이브리드"가 이 실패 유형의 근본 해법에 더 가깝다고 판단(BM25는
"IEEE Std 142"라는 정확한 문자열을 그대로 매칭하므로 임베딩의 의미론적 한계를
구조적으로 보완함). 다만 착수 시점은 Phase 2 완료 후로 유지.

### M-6. 다음 단계
Phase 2(나머지 27개 파일에 대한 A/B 유형 가벼운 커버리지 확장)로 진행.

---

## N. 도메인 분류 프롬프트 — 확장 시도와 회귀, 그리고 구조적 해법으로의 선회

### N-1. 편향 진단(bias_probe) 결과 — 걱정했던 방향과 실제 문제가 달랐음
M�� 이후 "admin 쪽 예시만 계속 추가하면 반대방향(technical→admin 오분류) 편향이
생기지 않을까"라는 우려를 검증하기 위해 반대방향 3문항 + kW임계값 패턴 3문항
(`golden_dataset_bias_probe.csv`)을 추가해 테스트.
**결과**: 반대방향 3문항은 전부 정확(우려했던 편향 없음). 대신 admin→technical
오분류가 **kW/kV + 선임형태(상주/대행) 판단**, **"기술" 키워드 + 자료요청** 두
패턴에서 반복적으로 재현됨(P2-32, P2-33, BP-04 3건).

### N-2. 프롬프트 확장 시도 — 목표는 달성했으나 심각한 회귀 발생
위 두 패턴을 겨냥해 `CLASSIFIER_SYSTEM`에 규칙과 예시(발전설비 300kW 선임형태,
기술질의 회신사례집)를 추가. 86개 전체 재평가 결과:
- **목표 달성**: P2-32, P2-33, BP-04 전부 수정됨(F유형 전체 12/12, 100%).
- **심각한 회귀 발생**: phase2의 도메인 정확도가 96.3%→83.3%로 하락. "OCR 탭
  계산", "특고압 인입선로(22,900V)", "-20~-45℃ 급냉실 등기구", "전선굵기 선정
  기준" 등 **admin 냄새나는 단어가 전혀 없는 순수 기술질문 9건**이 새로 admin으로
  잘못 분류됨.
**추정 원인**: 추가한 예시에 "kW", "kV", 숫자+단위, "기준"이라는 표현이 반복되면서,
"숫자+단위+기준"이라는 표면적 패턴 자체가 의도한 것보다 훨씬 넓게 "admin 신호"로
일반화된 것으로 보임(22,900V·-20~45℃ 등 전혀 다른 맥락의 숫자·단위에도 반응).
**순손실 계산**: 수정 전 86개 중 3개 실패 → 수정 후 12개 실패(3개 해결, 9개 신규
발생). net-negative로 판단해 **즉시 되돌림**(N-1 이전 상태로 복원, 계량기 요금/원리
구분 예시만 유지).

### N-3. 결론 — 이 실패가 오히려 처음부터 제안됐던 구조적 해법의 근거가 됨
이번 세션 초반(top_k 동적할당/doc_grade 선별 논의 때) "여러 판단을 한 프롬프트에
몰아넣으면 서로 간섭한다"는 이유로 도메인 분류를 `classify()`에서 분리해 독립
에이전트로 만드는 방향을 제안했었으나, 실행 우선순위상 미착수 상태였음. N-2의
회귀가 그 우려를 실증한 사례임 — 하나의 공유 프롬프트에 예시를 계속 추가하는
방식은 국소적 수정처럼 보여도 전혀 무관한 다른 케이스에 영향을 준다는 것이
데이터로 확인됨.
**결정**: 도메인 분류 관련 추가 수정은 지금 방식(단일 `CLASSIFIER_SYSTEM`에 예시
누적)으로 계속하지 않기로 함. [PENDING] 도메인 분류를 `check_urgency`처럼 독립된
전용 LLM 호출로 분리하는 것을 다음 우선 작업으로 재상정. 분리 후에는 예시를
추가해도 다른 필드(type/category) 판단이나 무관한 도메인 판단에 간섭하지 않을
것으로 기대되며, 이 가설도 분리 후 골든셋(특히 bias_probe)으로 재검증 필요.