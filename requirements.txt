fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic
pydantic-settings
python-dotenv==1.0.1

# --- LLM: 현재 CLOVA Studio (requests 로 직접 호출) ---
requests==2.32.3
# google-genai==2.10.0   # (보존) Gemini 로 되돌릴 때 주석 해제

chromadb==1.5.9
sentence-transformers

# --- DB: 문의 이력 저장 (PostgreSQL) ---
sqlalchemy
psycopg2-binary

# --- 인증: JWT 로그인 ---
# (passlib는 최신 bcrypt와 호환성 버그가 있어 bcrypt를 직접 사용)
bcrypt
python-jose[cryptography]
python-multipart

# --- LangGraph (파이프라인 오케스트레이션) ---
langgraph
langchain-core
EOF